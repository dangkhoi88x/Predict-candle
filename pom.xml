<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.example</groupId>
    <artifactId>Candles</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>Candles</name>
    <description>Candles</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>25</java.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-json</artifactId>
        </dependency>

        <!-- The starter, not flyway-core on its own: Boot 4 keeps the Flyway auto-configuration
             in its own module, so the bare library sits on the classpath and never runs. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-gson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.cloudinary</groupId>
            <artifactId>cloudinary-http5</artifactId>
            <version>2.4.0</version>
        </dependency>
        <dependency>
            <groupId>org.web3j</groupId>
            <artifactId>crypto</artifactId>
            <version>5.0.1</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!--
                Minifies the front end's own scripts into target/classes, so the jar ships them
                minified and a checkout still runs the sources it can read. Measured on app.js:
                66 KB to 25 KB, and 19.5 KB to 8.5 KB gzipped — gzip alone does not get close,
                because renaming locals is not something a compressor can do.

                Closure rather than esbuild, which is 1 KB better on this input, because esbuild
                needs Node in the build image: the Docker build runs Maven alone, and one tool
                that is already a Maven artifact beats a second toolchain downloaded per build.

                In place (allowReplacingInputFiles) and under the same names, because index.html
                and admin.html name these files and AppShellService joins them by those names.
                The two committed bundles are excluded: vite already minified them, and putting
                4.5 MB through Closure would add minutes to every build for nothing.
            -->
            <plugin>
                <groupId>com.github.blutorange</groupId>
                <artifactId>closure-compiler-maven-plugin</artifactId>
                <version>2.28.0</version>
                <executions>
                    <execution>
                        <id>minify-static-js</id>
                        <phase>prepare-package</phase>
                        <goals><goal>minify</goal></goals>
                        <configuration>
                            <baseSourceDir>${project.build.outputDirectory}/static</baseSourceDir>
                            <baseTargetDir>${project.build.outputDirectory}/static</baseTargetDir>
                            <sourceDir>.</sourceDir>
                            <targetDir>.</targetDir>
                            <includes><include>*.js</include></includes>
                            <excludes>
                                <exclude>wallet-auth.js</exclude>
                                <exclude>blog-editor.js</exclude>
                            </excludes>
                            <outputFilename>#{path}#{basename}.#{extension}</outputFilename>
                            <allowReplacingInputFiles>true</allowReplacingInputFiles>
                            <skipMerge>true</skipMerge>
                            <!-- SIMPLE renames locals and drops dead code; ADVANCED rewrites
                                 property names, which would break every window.CandleX global
                                 and every element id these files reach for. -->
                            <closureCompilationLevel>SIMPLE_OPTIMIZATIONS</closureCompilationLevel>
                            <closureLanguageIn>ECMASCRIPT_2020</closureLanguageIn>
                            <closureLanguageOut>ECMASCRIPT_2017</closureLanguageOut>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
